version: '3.8'

services:
  ffmpeg-go:
    build: .
    ports:
      - "8082:8082"
    volumes:
      - ./data:/app/data
      - ./temp:/app/temp
    environment:
      - PORT=8082
      - MAX_WORKERS=4
    restart: unless-stopped